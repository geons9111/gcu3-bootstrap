{
  "version": "2.0.0",
  "tasks": [
    { "label": "docker: compose up", "type": "shell", "command": "docker compose -f docker/docker-compose.yml up -d", "problemMatcher": [] },
    { "label": "docker: compose down", "type": "shell", "command": "docker compose -f docker/docker-compose.yml down", "problemMatcher": [] },
    { "label": "docker: status(manage)", "type": "shell", "command": "bash integration/linux/21_docker_manage.sh status", "problemMatcher": [] },
    { "label": "py: venv + install", "type": "shell", "command": "python3 -m venv .venv && . .venv/bin/activate && python -m pip install -U pip setuptools wheel && pip install -e .", "problemMatcher": [] },
    { "label": "test: pytest", "type": "shell", "command": ". .venv/bin/activate && pytest -q", "group": "test", "problemMatcher": [] }
  ]
}
